package com.daojie.domain;

/*
枚举值一般都是大写，多个枚举值直接用逗号隔开
 */
// 定义一个Direction的枚举类型，引用数据类型
public enum Direction {
    // Direction是枚举类型，对应的值是UP,DOWN,LEFT,RIGHT
    UP,
    DOWN,
    LEFT,
    RIGHT
    // 使用枚举
    // 定义一个Direction枚举类型的变量direction，该变量的值是Direction.UP;
    // Direction direction = Direction.UP;
}
